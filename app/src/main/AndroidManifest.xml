<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.liubin.design">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".main.MainActivity"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".defalt.DefaultActivity" />
        <activity android:name=".text_input_layout.TextInputLayoutActivity" />
        <activity android:name=".floating_action_button.FloatingActionButtonActivity" />
        <activity android:name=".material.MaterialActivity" />
        <activity android:name=".test.TestActivity" />
    </application>

</manifest>